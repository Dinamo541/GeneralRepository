#include <iostream>

bool NQueens(int n) {
  int** tablero = int*[n];
  for (int i = 0; i < n; i++) {
    int* tablero = int[n];
  }
  if (solve(tablero, 0)) {
    for (int i = 0; i < n; i++) {
      for (int j = 0; j < n; j++) {
        std::cout << tablero[i]
      }
    }
}

bool solve(int** tablero, int columna) {
  if (
}

bool esSeguro(int** tablero, ) {
    for (int i = 0; i < 
}
  
